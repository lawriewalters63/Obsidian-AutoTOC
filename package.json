{
  "name": "obsidian-autotoc",
  "version": "1.0.4",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "dev": "node esbuild.config.mjs",
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate-release.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.7.2"
  }
}
